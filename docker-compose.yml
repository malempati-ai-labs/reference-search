services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - QDRANT_URL=http://vector_db:6333
    volumes:
      - ./src:/app/src
  vector_db:
    image: qdrant/qdrant
    ports:
      - "6333:6333"